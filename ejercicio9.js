// Ejercicio 9: Número Positivo/Negativo
let n = -3; // Definimos un número

if (n > 0) { // Verificamos si el número es mayor a 0
  console.log("Positivo"); // Si es mayor, es positivo
} else if (n < 0) { // Si el número es menor a 0
  console.log("Negativo"); // Mostramos que es negativo
} else {
  console.log("Cero"); // Si no es mayor ni menor, es cero
}