// 61. Suma de Elementos
let numeros = [1, 2, 3, 4, 5]; // Array de números
let suma = 0; // Acumulador
for (let i = 0; i < numeros.length; i++) { // Recorre array
  suma += numeros[i]; // Suma cada elemento
}
console.log(suma); // Muestra resultado